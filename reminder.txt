`restaurant` is the app_name

`web_restaurant` is the project_name

`web_restaurant` have settings.py

restaurant > templates > restaurant   ==> contains the htmls files
restaurant > static > restaurant   ==> contains the css, js, images files

run server- python manage.py runserver
end server- deactivate

####
modify:
settings.py -- 'debug': True,  # Ensure debug is True for development           <added>
settings.py -- 'DIRS': [],    ==>     'DIRS': [BASE_DIR / 'templates'],         <update>

created:
urls.py         in restaurant   <app>
